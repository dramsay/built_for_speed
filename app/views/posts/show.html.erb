<h1><%= h @post.title %></h1>

<%= image_tag @post.image.url(:large), :class => 'left' %>
<%= simple_format(h(@post.body)) %>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>